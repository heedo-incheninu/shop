# 쇼핑몰 디자인 기준

## 문서의 목적

이 문서는 `references/screens/`의 실제 쇼핑몰 화면에서 확인한 시각 규칙을
정리한 것이다. 기능과 데이터 범위는 `SHOP.md`를 따르고, 화면을 구현할 때는
이 문서의 색, 글자 위계, 여백, 선, 모서리와 배치를 기준으로 삼는다.

현재 참고 화면은 모두 데스크톱 화면이다. 모바일 화면, 브레이크포인트와
모바일 재배치는 참고 근거가 없으므로 정하지 않는다.

## 참고 화면과 측정 기준

| 파일 | 대상 화면 | 캡처 크기 |
|---|---|---:|
| `references/screens/home.png` | 상품 목록 | 1106 × 797px |
| `references/screens/detail.png` | 상품 상세 | 1270 × 831px |
| `references/screens/cart.png` | 장바구니 | 1288 × 847px |

세 PNG에는 약 120dpi 정보가 들어 있다. 이 정보만으로 브라우저의 확대율을
확정할 수는 없지만, 반복해서 나타나는 치수를 0.8배 했을 때 일반적인 CSS
정수값에 가까워진다. 따라서 이 문서에서는 다음 두 값을 구분해 적는다.

- **캡처 실측** — 이미지에서 직접 센 raw px. 확인된 근거다.
- **CSS 후보** — 125% 배율로 캡처되었다고 보고 `raw px × 0.8`로 계산한 값.
  구현을 시작할 때 쓰는 후보이며, 참고 화면과 나란히 비교해 확정한다.
- 1px 선은 배율과 관계없이 CSS에서도 1px 후보로 유지한다.
- 범위로 적은 값과 `약`이 붙은 값은 래스터 이미지에서 정확한 원 CSS 값을
  판별할 수 없다는 뜻이다.

## 가져오는 것과 가져오지 않는 것

### 가져오는 것

- 흰 바탕, 짙은 글자와 얇은 회색 선으로 구획하는 평면적인 구성
- 상품 목록의 왼쪽 분류 영역과 4열 상품 격자
- 상품 상세의 큰 이미지와 상품 정보 2열 배치
- 장바구니의 상품 목록과 주문 요약 2열 배치
- 글자 크기와 굵기로 이름, 가격, 합계를 구분하는 위계
- 각진 목록·상세 요소와 작게 둥근 장바구니 카드 및 조작기

### 가져오지 않는 것

- 참고 사이트의 로고, 브랜드 이름, 브랜드 파란색과 브랜드 아이콘
- 참고 사이트의 상품 사진과 문구
- 검색, 상세 필터, 메가 메뉴, 프로모션 배너와 정렬 기능
- 할인율, 취소선 가격, 별점·리뷰, 배송·적립 문구와 배지
- 찜·공유, 상품 옵션, 재고 임박 안내와 오른쪽 미니 장바구니
- 배송비, 쿠폰, 할인과 부분 선택 주문
- 장식용 그림, 성공 아이콘, 그라데이션과 반투명 카드

## 공통 디자인 토큰

### 색

아래 색만 화면 UI에 사용한다. 상품 사진 안의 색은 디자인 토큰이 아니다.

| 역할 | 값 | 근거와 사용처 |
|---|---|---|
| 기본 표면 | `#FFFFFF` | 모든 화면의 본문과 카드 |
| 바깥 배경 | `#F0F0F0` | 장바구니 화면의 상단·바깥 영역 |
| 약한 구분 배경 | `#F2F4F6` | 장바구니 행 사이 띠, 비활성 버튼 |
| 강한 제목 | `#131516` | 장바구니의 큰 페이지 제목 |
| 본문·주요 액션 | `#232B35` | 본문, 활성 상태, 주요 버튼 |
| 보조 문구 | `#667380` | 설명과 부가 정보 |
| 비활성 문구 | `#ACB5BF` | 비활성 단계와 버튼 |
| 기본 선 | `#E0E3E7` | 카드 외곽선과 구분선 |
| 조작기 선 | `#C6CDD4` | 수량 조절기와 세부 구분선 |
| 가격·경고 | `#BA2D1B` | 현재 가격과 오류·경고 문구 |

- 주요 버튼은 `#232B35` 배경에 `#FFFFFF` 글자를 쓴다.
- 활성 분류는 `#232B35` 글자와 굵기 700으로 표시한다.
- 비활성 주문 버튼은 `#F2F4F6` 배경과 `#ACB5BF` 글자를 쓴다.
- 참고 화면에 있더라도 브랜드 파란색, 별점 주황색과 배송 초록색은 쓰지
  않는다.

### 글꼴과 글자 위계

- 외부 글꼴을 불러오지 않고 CSS의 `sans-serif`를 사용한다.
- 일반 글자는 굵기 400, 제목·가격·버튼은 굵기 700을 사용한다.
- 캡처만으로 600과 700을 확실히 구분할 수 없으므로 중간 굵기는 만들지
  않는다.

| 역할 | 캡처 실측 | CSS 후보 | 굵기 |
|---|---:|---:|---:|
| 작은 보조 문구 | 10–12px | 8–10px | 400 |
| 상품 목록 이름 | 12–13px | 10px 안팎 | 400 |
| 일반 본문·상품명 | 13–16px | 10–13px | 400 |
| 목록 가격 | 약 16px | 약 13px | 700 |
| 상세 가격·중간 제목 | 18–22px | 14–18px | 700 |
| 장바구니 진행 단계 | 20–22px | 16–18px | 700 |
| 장바구니 상품가·총액 | 26–28px | 21–22px | 700 |
| 장바구니 페이지 제목 | 40–42px | 32–34px | 700 |

글꼴에 따라 실제 글리프 크기가 달라진다. 구현할 때는 CSS 후보를 시작값으로
쓰고 캡처와 나란히 비교해 시각 크기를 맞춘다.

### 선, 모서리와 그림자

- 기본 외곽선과 구분선은 1px이다.
- 상품 목록 카드와 상품 상세의 주요 영역은 모서리 0px이며 그림자를 쓰지
  않는다.
- 장바구니 카드, 수량 조절기와 버튼은 캡처 실측 5–6px, CSS 후보 4–5px의
  모서리를 사용한다.
- 장바구니 상품 행 사이의 넓은 구분 띠는 캡처 실측 10px, CSS 후보 8px이며
  색은 `#F2F4F6`이다.
- 참고 화면에서 그림자가 확인되는 분류 펼침 메뉴는 이번 범위에서 제외되므로
  공통 그림자 값은 만들지 않는다.

### 상품 이미지

- `SHOP.md`의 상품 표가 지정한 `references/products/` 파일만 사용한다.
- 모든 목록·상세·장바구니 이미지 영역은 1:1 정사각형이다.
- 이미지에는 `object-fit: contain`을 적용하고 중앙에 배치한다.
- 배경은 `#FFFFFF`이며 사진을 자르거나 다시 축소하지 않는다.
- 이미지에 테두리, 그림자, 둥근 마스크와 대체 장식을 추가하지 않는다.

## 상품 목록

근거 화면은 `references/screens/home.png`이다.

### 배치

- 데스크톱에서 왼쪽에는 분류, 오른쪽에는 상품 격자를 둔다.
- 왼쪽 영역은 캡처 실측 약 166px, CSS 후보 약 133px이다.
- 왼쪽 영역과 상품 영역 사이 간격은 캡처 실측 약 17px, CSS 후보 약 14px이다.
- 상품 영역은 캡처 실측 900px, CSS 후보 720px이다.
- 상품은 한 줄에 4개를 같은 너비로 배치한다.

| 요소 | 캡처 실측 | CSS 후보 |
|---|---:|---:|
| 상품 한 칸 | 225px | 180px |
| 이미지 슬롯 | 약 181–183px 정사각형 | 약 145–146px 정사각형 |
| 카드 안 좌우 여백 | 약 21–22px | 약 17–18px |
| 이미지와 상품명 간격 | 약 18–20px | 약 14–16px |

### 내용과 상태

- 분류 제목과 `잡화`·`뷰티`·`신발`·`식품` 네 항목만 표시한다.
- 선택한 분류는 `#232B35`, 굵기 700으로 표시한다. 선택하지 않은 분류는
  같은 색에 굵기 400을 사용해 별도 브랜드 색을 만들지 않는다.
- 상품 카드에는 이미지, 이름과 현재 가격만 표시한다.
- 카드에는 외곽선, 배경색, 모서리와 그림자를 넣지 않는다.
- 필터, 배너, 정렬 바, 할인, 별점·리뷰, 배송·적립 정보가 있던 자리는
  장식으로 채우지 않고 제거한다.

## 상품 상세

근거 화면은 `references/screens/detail.png`이다.

### 배치

- 큰 상품 이미지와 상품 정보의 2열 구조를 사용한다.
- 이미지 영역은 캡처 실측 약 420px, CSS 후보 약 336px이다.
- 상품 정보 영역은 캡처 실측 548px, CSS 후보 약 438px이다.
- 이미지 영역은 정사각형으로 두고 상품을 `contain`으로 중앙 정렬한다.
- 두 영역은 흰 바탕 위에 평평하게 배치하며 카드 외곽선과 그림자를 넣지
  않는다.

### 정보 위계와 조작

- 상품 정보는 이름, 가격, 설명, 수량 조절기, 장바구니 담기 버튼 순서로
  배치한다.
- 정보 구획 사이에는 `#E0E3E7` 1px 선을 사용한다.
- 상세 가격은 `#BA2D1B`, 굵기 700을 사용한다.
- 수량 조절기는 장바구니와 같은 캡처 실측 140 × 40px, CSS 후보
  112 × 32px 규칙을 재사용한다.
- 수량 감소, 현재 수량, 수량 증가를 한 줄에 배치한다.
- 장바구니 버튼은 상품 정보 영역의 전체 너비를 사용한다. 참고 화면의
  전체 너비 조작기 높이인 캡처 실측 33px, CSS 후보 약 26px를 시작값으로
  쓰며 배경은 `#232B35`, 글자는 `#FFFFFF`로 한다.
- 리뷰, 구매 수, 배송 정보, 적립, 옵션 탭, 찜·공유와 미니 장바구니는
  표시하지 않는다.

## 장바구니

근거 화면은 `references/screens/cart.png`이다.

### 전체 배치

- 바깥은 `#F0F0F0`, 실제 본문은 `#FFFFFF`로 구분한다.
- 콘텐츠 전체 폭은 캡처 실측 약 1176px, CSS 후보 약 940px이다.
- 왼쪽 상품 목록과 오른쪽 주문 요약의 2열 구조를 사용한다.

| 요소 | 캡처 실측 | CSS 후보 |
|---|---:|---:|
| 상품 목록 열 | 약 788px | 약 630px |
| 두 열 사이 간격 | 약 14px | 약 11px |
| 주문 요약 열 | 약 375px | 약 300px |
| 카드 안쪽 여백 | 약 25–26px | 약 20–21px |
| 상품 이미지 슬롯 | 약 176px 정사각형 | 약 141px 정사각형 |
| 수량 조절기 | 약 140 × 40px | 약 112 × 32px |
| 요약 버튼 | 약 323 × 56px | 약 258 × 45px |

### 상품 목록

- 각 행에는 상품 이미지, 이름, 가격, 수량 조절기와 삭제 버튼만 둔다.
- 부분 선택 주문 기능이 없으므로 상품 선택 체크박스와 선택 안내 카드는
  표시하지 않는다.
- 상품 행 사이에는 `#F2F4F6` 구분 띠를 둔다.
- 수량 조절기에는 `#C6CDD4` 1px 선과 CSS 후보 4–5px 모서리를 사용한다.
- 배송일, 배송비, 재고, 할인, 배지와 적립 정보는 표시하지 않는다.

### 주문 요약

- 요약 카드에는 `#E0E3E7` 1px 선과 CSS 후보 4–5px 모서리를 사용한다.
- 배송비나 할인 행을 만들지 않고 `총 상품 가격`과 최종 합계만 표시한다.
- 최종 합계는 가장 큰 금액 위계와 굵기 700을 사용한다.
- 장바구니에 상품이 있으면 주문 버튼을 `#232B35` 배경과 `#FFFFFF` 글자로
  표시한다.
- 장바구니가 비어 있으면 같은 크기의 버튼을 `#F2F4F6` 배경과
  `#ACB5BF` 글자로 비활성화한다.

## 주문 완료

주문 완료 전용 참고 화면은 없다. 새 색이나 구성 요소를 만들지 않고
장바구니 화면의 규칙을 그대로 재사용한다.

- 장바구니와 같은 페이지 제목 위계와 2열 폭을 사용한다.
- 왼쪽 카드에는 주문한 상품의 이미지, 이름, 구매 당시 가격과 수량을
  표시한다.
- 오른쪽 요약 카드에는 주문 번호와 주문 금액을 표시한다.
- 성공 아이콘, 축하 배너, 배송 정보와 추천 상품을 추가하지 않는다.
- 카드 선, 모서리, 안쪽 여백과 행 구분은 장바구니 규칙과 같다.

## 상단 영역과 브랜드

참고 화면의 로고, 검색창, 카테고리 버튼과 서비스 메뉴는 이 프로젝트의
기능·브랜드 근거가 아니다.

- 참고 사이트의 로고, 이름, 아이콘과 브랜드 색을 사용하지 않는다.
- 프로젝트의 서비스 이름과 로고가 정해지기 전에는 별도 로고 규격을 만들지
  않는다.
- 검색 기능과 메가 메뉴는 `SHOP.md` 범위가 아니므로 만들지 않는다.
- 필요한 화면 이동은 텍스트 링크와 각 화면의 기능 버튼으로 제공하며,
  디자인 토큰은 이 문서의 중립 색만 사용한다.

## 반응형 상태

- 현재 문서가 확정하는 범위는 데스크톱뿐이다.
- 참고 화면보다 좁은 폭에서 열 수, 순서, 여백과 글자 크기를 어떻게 바꿀지는
  정하지 않는다.
- 임의의 모바일 브레이크포인트나 한 열 재배치를 추가하지 않는다.
- 모바일 참고 화면이 제공되면 그 화면에서 값을 다시 측정한 뒤 이 문서를
  먼저 갱신하고 구현한다.

## 구현 후 시각 검증

- 화면을 대응하는 참고 이미지와 나란히 놓고 전체 폭, 열 비율, 이미지 크기,
  글자 위계와 여백을 비교한다.
- CSS 후보로 시작한 값은 비교 결과로 검증하고, 차이가 있으면 먼저 보고한다.
  이 문서의 값을 바꿔야 할 때는 문서 수정 승인을 별도로 받는다.
- 화면에 사용된 모든 UI 색이 이 문서의 색 표에 있는지 확인한다.
- 상품 이미지가 모두 정사각형 영역 안에서 잘리지 않고 보이는지 확인한다.
- 참고 브랜드 요소와 제외한 기능이 화면에 남아 있지 않은지 확인한다.
- 상품 목록, 상세, 장바구니와 주문 완료가 `SHOP.md`의 정보만 표시하는지
  확인한다.
